@model IEnumerable<MedistockAppCore.Models.Category>

@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.NameC)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.NameC)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdCategory">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdCategory">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdCategory">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
</body>
</html>
